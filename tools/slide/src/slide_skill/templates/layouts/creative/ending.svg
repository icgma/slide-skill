<svg width="1280" height="720" viewBox="0 0 1280 720" xmlns="http://www.w3.org/2000/svg">
  <g id="background"><rect x="0" y="0" width="1280" height="720" fill="{background}" /></g>
  <g id="content-title-NN">
    <text x="640" y="280" font-family="{font}" font-size="80" font-weight="700" fill="{accent}" text-anchor="middle">Thanks!</text>
    <text x="640" y="380" font-family="{font}" font-size="24" fill="{text_secondary}" text-anchor="middle">Let's connect</text>
    <text x="640" y="440" font-family="{font}" font-size="18" fill="{text_tertiary}" text-anchor="middle">@handle · email@domain.com · website.com</text>
  </g>
  <g id="chrome-stripe"><rect x="0" y="0" width="6" height="720" fill="{accent}" /></g>
  <g id="chrome-footer">
    <rect x="0" y="688" width="1280" height="32" fill="{surface}" />
    <text x="1184" y="708" font-family="{font}" font-size="12" fill="{muted}" text-anchor="end">NN / TT</text>
  </g>
</svg>
