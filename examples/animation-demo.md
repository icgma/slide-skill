# 动画演示

## 淡入动画

这个标题会以淡入效果出现。

## 飞入动画

内容将以飞入动画呈现，模拟逐条展开的效果。

- 第一点：从左飞入
- 第二点：延迟 200ms 后飞入
- 第三点：延迟 400ms 后飞入

## 擦除转场

页面之间的转场效果可以用 wipe、fade、push 等方式切换。

## 缩放动画

以缩放效果展示重点内容。

## 总结

- 页面转场：fade、wipe、push、split、zoom
- 元素动画：fly-in、fade-in、wipe、zoom-in、float-in
- 通过 SVG data 属性控制
