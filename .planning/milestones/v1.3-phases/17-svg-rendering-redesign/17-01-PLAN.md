# Phase 17 Plan: SVG Rendering Engine Redesign

**Created:** 2026-05-02
**Execution:** Google Jules (via jules-dispatch)

## Plan

### Plan 17-01: SVG Rendering Redesign (5 subtasks, sequential)

**Subtask 1: Fix text layout — bullet points and line breaks**
- File: `svg_pipeline.py`
- Fix `_markdown_to_slides()` to preserve list items as separate lines
- Fix `_render_slide_svg()` to render each line as its own `<tspan>`
- Bullet lines get `• ` prefix, empty lines create spacing
- Verify: slide_03.svg shows bullets on separate lines
- Executor: Google Jules
- Task file: `jules-dispatch/tasks/slide-skill-svg-redesign.yaml` (task 1)

**Subtask 2: Fix title overflow and empty body content**
- File: `svg_pipeline.py`
- Title auto-wrap: >20 chars splits into 2 tspan, font reduced to 36px
- Empty body: centered title layout, skip white card
- Verify: slide_01.svg has centered title, no empty card
- Executor: Google Jules
- Dependency: Subtask 1

**Subtask 3: Add multiple layout templates**
- File: `svg_pipeline.py`
- New function `_select_layout()` returns: section_divider, bullet_list, two_column, metric_highlight, default
- 4 new layout renderers + selection logic
- Verify: different slides use different layouts
- Executor: Google Jules
- Dependency: Subtask 2

**Subtask 4: Improve visual design polish**
- File: `svg_pipeline.py`
- Global chrome: footer bar, accent stripe, progress dots, title underline, card shadow
- Verify: all layouts include chrome elements, SVG QA passes
- Executor: Google Jules
- Dependency: Subtask 3

**Subtask 5: Integration test**
- Full pipeline: init → spec → svg → check → finalize → export → validate
- Competition toolkit: competitions, draft-notes, rehearse
- Output: INTEGRATION-TEST-RESULTS.md
- Executor: Google Jules
- Dependency: Subtask 4

## Verification

- All `pytest tests/` pass
- `slide-skill check-svg` passes
- SVG output has proper bullet formatting, title wrapping, layout variety, visual polish
- PPTX export produces valid native shapes
