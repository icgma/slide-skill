# Plan 08-01: SVG Path Geometry Implementation

**Phase:** 8
**Created:** 2026-05-01
**Status:** Ready

## Objective

Implement progressive SVG path/curve/arc/polygon conversion to native editable PPTX freeform shapes with an extensible converter registry.

## Verification

- All 8 GEO requirements verified
- Full test suite passes (`python -m unittest discover -s tests -v`)
- New geometry tests cover all SVG path command types

## Steps

### Wave 1: Foundation (sequential)

1. **Add svgpathtools dependency** — update `pyproject.toml` with `svgpathtools>=1.6`
2. **Create `geometry.py`** — SVG path parsing (via svgpathtools) + DrawingML freeform XML construction
3. **Create `converters.py`** — ConverterRegistry class + converter functions for path, polygon, polyline

### Wave 2: Integration (sequential, depends on Wave 1)

4. **Modify `exporter.py`** — Replace `_iter_drawables` tag switch with registry dispatch; refactor existing primitive handlers into converter callables
5. **Modify `svg_pipeline.py`** — Move path/polygon/polyline to SUPPORTED_DRAWABLE_TAGS; add attribute validation for path `d`, polygon/polyline `points`

### Wave 3: Testing (parallel)

6. **Create `tests/test_geometry.py`** — Test all SVG path command types (M/mLlCcSsQqTtAaZz), polygon, polyline, round-trip verification
7. **Update `tests/test_pipeline.py`** if needed — Verify existing pipeline test still passes with registry refactor

### Wave 4: Verification (sequential)

8. **Run full test suite** — All existing + new tests pass
9. **Commit artifacts** — PLAN.md, SUMMARY.md, VERIFICATION.md
