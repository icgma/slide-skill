---
phase: 4
plan: 01
status: complete
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-05]
---

# Plan 04-01: Native PPTX Export

## Objective

Create a tested SVG-to-PPTX export path with validation and backup artifacts.

## Tasks

- Implement `exporter.py`.
- Add `export`, `validate-pptx`, and `pptx-text` CLI commands.
- Preserve notes sidecar.
- Archive SVG output/final artifacts.
- Add native-editability tests.

## Verification

- `slide-skill quickstart examples/demo.md --name smoke-demo`
- `python -m unittest discover -s tests -v`
