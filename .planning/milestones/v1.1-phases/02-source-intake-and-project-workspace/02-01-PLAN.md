---
phase: 2
plan: 01
status: complete
requirements: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05, SRC-06]
---

# Plan 02-01: Source Intake And Workspace

## Objective

Create a reusable project workspace API and source-to-Markdown converters.

## Tasks

- Implement `project.py`.
- Implement `intake.py`.
- Expose `init`, `import-sources`, `validate`, `source-to-md`, and `pptx-text` CLI commands.
- Add source intake tests.

## Verification

- `python -m unittest discover -s tests -v`
- `slide-skill init --help`
- `slide-skill source-to-md --help`
